FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY agent/risk_control_pipeline ./risk_control_pipeline

CMD ["adk", "api_server", "--host", "0.0.0.0", "--port", "9000", "."]
